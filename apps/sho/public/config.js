// The Sho — runtime config. Edit this file from the GitHub web editor after
// following DEPLOY.md. Leave values empty to run in demo mode (no backend).
window.SHO_CONFIG = {
  // Supabase Dashboard → Project Settings → API → Project URL
  supabaseUrl: '',
  // Supabase Dashboard → Project Settings → API → anon public key (safe to expose)
  supabaseAnonKey: '',
  // Printed by the push edge function's one-time setup step (DEPLOY.md step 5)
  vapidPublicKey: '',
}
